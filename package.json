{
  "name": "kevala",
  "license": "UNLICENSED",
  "engines": {
    "node": "14.15",
    "yarn": "1.22"
  },
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "web-test-runner",
    "test:watch": "web-test-runner --watch",
    "cover": "web-test-runner --coverage",
    "cover:html": "web-test-runner --coverage && ws -p ${PORT:-3030} -d coverage/lcov-report --spa index.html"
  },
  "devDependencies": {
    "@snowpack/web-test-runner-plugin": "^0.1.5",
    "@web/test-runner": "^0.11.6",
    "chai": "^4.2.0",
    "local-web-server": "^4.2.1",
    "snowpack": "^2.18.5"
  }
}
