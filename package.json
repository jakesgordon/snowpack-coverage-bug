{
  "name": "bug",
  "license": "UNLICENSED",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "web-test-runner",
    "cover": "web-test-runner --coverage && ws -p 3030 -d coverage/lcov-report --spa index.html"
  },
  "devDependencies": {
    "@snowpack/web-test-runner-plugin": "^0.1.5",
    "@web/test-runner": "^0.11.6",
    "chai": "^4.2.0",
    "local-web-server": "^4.2.1",
    "snowpack": "^2.18.5"
  }
}
